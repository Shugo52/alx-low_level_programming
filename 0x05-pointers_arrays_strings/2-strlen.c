#include "main.h"
#include <stdlib.h>

/**
 * _strlen - returns string length
 *
 * @s: character
 * @n - integer
 *
 * Return: returns lenght
 */
int _strlen(char *s)
{
	int n;

	n = strlen(*s);
	return (n);
}
